# Core libraries for data manipulation and numerical operations
pandas==2.0.3
numpy==1.25.2

# Libraries for data visualization
matplotlib==3.8.0
seaborn==0.12.2

# Machine learning libraries
scikit-learn==1.3.0

# Library for SQL database interaction
sqlite3  # Typically included with Python, so this line can be omitted if sqlite3 is part of the standard library